# Run Tests

Run tests for the specified area.

$ARGUMENTS

## If no arguments provided:
- Frontend: `cd frontend && pnpm lint`
- Backend: `cd backend && go vet ./...`

## After running:
1. Report pass/fail status
2. If failures, show first 3 errors
3. Suggest fixes for failures
